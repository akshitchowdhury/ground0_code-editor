# Terraform state can contain the generated DB password — never commit it.
*.tfstate
*.tfstate.*
.terraform/
.terraform.lock.hcl
crash.log

# Local variable files (may hold identifiers you'd rather not commit).
terraform.tfvars
*.auto.tfvars
